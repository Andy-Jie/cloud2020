#server:
#  port: 7001
#
#eureka:
#  instance:
#    hostname: eureka7001-server
#  client:
#    register-with-eureka: false
#    fetch-registry: false
#    service-url:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka

server:
  port: 7002

eureka:
  instance:
    hostname: eureka7002-server
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7001-server:7001/eureka